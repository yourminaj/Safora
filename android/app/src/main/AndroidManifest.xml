<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network: DisasterApiClient HTTP calls, Google Fonts runtime download -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- ConnectivityService: connectivity_plus checks network type -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- LocationService: Geolocator.getCurrentPosition() for SOS GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Background location for SOS when app is minimized (Android 10+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- NotificationService: flutter_local_notifications (Android 13+ / API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- AudioService: audioplayers keeps siren playing when screen is off -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- CountdownOverlay + DecoyCallScreen: HapticFeedback vibration -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Biometric authentication: local_auth fingerprint / face unlock -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Explicitly mark biometric hardware as OPTIONAL.
         Without this, Play Store infers the feature is required from the
         USE_BIOMETRIC permission and drops devices without fingerprint/face
         sensors. Safora falls back to PIN authentication on those devices. -->
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.biometrics"
        android:required="false"/>

    <!-- SmsService: telephony plugin sends SMS directly without opening Messages -->
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <!-- SOS Foreground Service: keeps SOS monitoring alive when app is in background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>

    <!-- VoiceDistressService: record package requires microphone access for PCM stream -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:label="Safora"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Google AdMob App ID (required by google_mobile_ads) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3413399953381965~6491284202"/>
        
        <!-- FCM default notification icon (white silhouette for status bar) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_safora"/>
        <!-- FCM default notification accent color (Safora primary red) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/safora_primary"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

